using UnityEngine;

/// <summary>
/// UIManager está pensado para manejar la interfaz de usuario general.
/// Ahorita está vacío, pero aquí podrías controlar botones, paneles,
/// menús, etc. sin mezclar esa lógica con el GameManager.
/// </summary>
public class UIManager : MonoBehaviour
{
    /// <summary>
    /// Start se llama una vez antes del primer Update.
    /// De momento no hace nada, pero lo dejamos listo
    /// por si más adelante quieres inicializar cosas de UI aquí.
    /// </summary>
    private void Start()
    {

    }

    /// <summary>
    /// Update se llama una vez por frame.
    /// Aquí podrías escuchar inputs para abrir/ cerrar menús,
    /// cambiar vistas, pausar el juego, etc.
    /// </summary>
    private void Update()
    {

    }
}
